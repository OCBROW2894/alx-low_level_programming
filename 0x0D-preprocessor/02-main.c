#include <stdio.h>

/**
 * main - where the compiler starts compiling
 * Description: print it's name
 * Return: Always 0 when correct.
 */

int main(void)
{
	printf("%s\n", __FILE__);

	return (0);
}
