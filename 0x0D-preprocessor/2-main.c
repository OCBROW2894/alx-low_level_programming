#include <stdio.h>

/**
 * main - prints it's self
 * Description: print the name of the source file
 * Return: Always 0 when correct.
 */

int main(void)
{
	printf("%s\n", __FILE__);

	return (0);
}
