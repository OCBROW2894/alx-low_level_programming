#ifndef FUNCTION_LIKE_MACRO_H
#define FUNCTION_LIKE_MACRO_H

/*
 * File: holds the formula
 * Desc:  a function-like macro ABS(x)that computes the
 *absolute value of a number x.
 */

#define ABS(x) ((x) < 0 ? (-x) : (x))

#endif
