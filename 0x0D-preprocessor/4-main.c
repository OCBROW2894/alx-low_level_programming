#include <stdio.h>
#include "4-sum.h"

/**
 * main - where the copiler starts compiling
 *
 * Return: Always 0 when correct.
 */
int main(void)
{
	int answer;

	answer = SUM(2800, 94);
	printf("%d\n", answer);
	return (0);
}
