#include <stdio.h>

/**
 * main - compiler starts copilling the code
 *
 * Return: Always 0 when correct.
 */
int main(void)
{
    printf("(A tortoise, having pretty good sense of a hare's nature, challenges one to a race.)\n");
    return (0);
}
